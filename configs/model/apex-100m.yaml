name: apex-100m
status: planned
purpose: Development-scale pretraining after suitable GPU hardware and datasets are available
vocab_size: 32000
hidden_size: 768
num_layers: 12
num_attention_heads: 12
num_key_value_heads: 4
intermediate_size: 2048
max_sequence_length: 2048
rope_theta: 10000.0
tie_word_embeddings: true
requires:
  - measured discrete GPU capacity
  - licensed versioned training dataset
  - tokenizer evaluation
  - checkpoint storage
