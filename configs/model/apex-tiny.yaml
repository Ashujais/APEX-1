name: apex-tiny
status: experimental
purpose: CPU-only architecture and pipeline smoke tests
vocab_size: 300
hidden_size: 64
num_layers: 2
num_attention_heads: 4
num_key_value_heads: 2
intermediate_size: 160
max_sequence_length: 128
rope_theta: 10000.0
tie_word_embeddings: true
